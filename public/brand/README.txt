Place the OFFICIAL Likhakriti logo files here, unmodified:
  logo.png        full wordmark (dark backgrounds)  — required
  logo-light.png  light-background variant          — optional
  symbol.png      standalone symbol (mobile/favicon) — optional
  favicon.ico, icon-192.png, icon-512.png
The UI loads them as-is and never applies effects or crops.
